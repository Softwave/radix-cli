{
  "name": "radix-cli",
  "version": "1.0.0",
  "description": "Command line program to convert number bases",
  "main": "bin/radix.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx tsc index.ts",
    "build": "npm run compile; noderify index.js --out bin/radix.js"
  },
  "author": "Jessica Leyba",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.13",
    "noderify": "^5.0.0",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "commander": "^6.2.0"
  },
  "bin": {
    "radix": "./bin/radix.js"
  }
}
